import { html, component, useState } from "../dependencies.js";
import { startGame } from "../game-engine.js";

export const GameHeader = ({ name }) => {
  return html`<div class="container"></div>`;
};
